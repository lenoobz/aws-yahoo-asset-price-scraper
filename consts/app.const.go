package consts

// Collection names
const (
	ASSETS_COLLECTION            = "assets"            // Should match with Colnames's key of AppConf
	ASSET_PRICES_COLLECTION      = "asset_prices"      // Should match with Colnames's key of AppConf
	SCRAPE_CHECKPOINT_COLLECTION = "scrape_checkpoint" // Should match with Colnames's key of AppConf
)

const PAGE_SIZE = 50
